version: '3.8'

services:
  app:
    image: ${DOCKER_HUB}/${IMAGE_NAMESPACE}/server-dashboard:latest
    build:
      context: ../..
      dockerfile: .gitlab-ci/build/Dockerfile
      args:
        DOCKER_HUB: ${DOCKER_HUB}
        IMAGE_NAMESPACE: ${IMAGE_NAMESPACE}
      platforms: [ "linux/amd64" ]
